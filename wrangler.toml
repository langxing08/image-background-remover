name = "image-background-remover"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".next"

# 环境变量（构建时注入，运行时通过 Cloudflare Dashboard 配置）
[vars]
NEXT_PUBLIC_API_URL = "/api"

# 路由配置：所有 /api/* 走 Worker，其余走静态
